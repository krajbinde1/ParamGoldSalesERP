<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- DEBUG-ONLY: adb trigger for production CriticalAlertNotifier path. -->
    <application>
        <receiver
            android:name=".ParamGoldTestAlertReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.mobile.TEST_CRITICAL_ALERT" />
            </intent-filter>
        </receiver>

        <!-- DEBUG-ONLY: allow adb am start for activity verification. -->
        <activity
            android:name=".CriticalAlertActivity"
            android:exported="true"
            tools:replace="android:exported" />
    </application>
</manifest>
